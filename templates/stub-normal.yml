---
meta:
  environment: NAME
  service_name: NAME
  security_groups:
    - default
  job_templates:
    - name: ecs_broker
      release: ecs_broker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID
releases:
  - name: ecs_broker
    version: latest
properties:
  ecs_broker: {}
